/** The Hono read-only API. Fixed by the review-ui brief. */
export const API_SERVER_PORT = 5170;

/** Vite dev server for the dashboard; proxies /api to API_SERVER_PORT. */
export const APP_DEV_PORT = 5173;
